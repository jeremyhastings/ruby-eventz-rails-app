<h1>Registrations for <%= link_to @event.name, @event %></h1>
<ul class="registrations">
  <% @registrations.each do | registration | %>
  <li>
    <%= link_to registration.user.name, registration.user %> &bull; <%= registration.how_heard %>
  </li>
  <% end %>
</ul>